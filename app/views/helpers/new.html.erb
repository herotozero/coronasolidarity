<%= simple_form_for @helper do |f| %>
    <%= render "#{@helper.current_step}_form", :f => f %>

    <%= f.button :submit, "Zurück", :name => "back_button" unless @helper.first_step? %>
    <%= f.button :submit, "Weiter", class: "btn-primary" %>

    <%# <%= f.button :submit, "Absenden", class: "btn-primary" %> %>
  <% end %>